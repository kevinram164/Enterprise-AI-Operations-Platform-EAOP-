# Umbrella defaults — override per environment in values-ocp1.npd.co.yaml

phoenix-infra:
  enabled: true
  storageClass: nfs-csi

platform-api:
  enabled: true
  image:
    repository: harbor.ocp1.npd.co/phoenix/platform-api
    tag: latest

admin-portal:
  enabled: true
  image:
    repository: harbor.ocp1.npd.co/phoenix/admin-portal
    tag: latest
