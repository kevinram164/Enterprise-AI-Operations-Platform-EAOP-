# Environment — OpenShift only

Copy to create secrets on cluster:

```bash
cp deploy/openshift/phoenix-platform/config.env.example .env
# edit passwords, then:
oc create secret generic platform-api-secret --from-env-file=.env -n phoenix-platform
```

See [deploy/README.md](deploy/README.md)
