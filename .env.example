# Environment — OpenShift + Harbor

```bash
cp deploy/openshift/phoenix-platform/config.env.example .env
```

Vault paths for CI/CD: see [ci/vault/README.md](ci/vault/README.md)

Harbor registry: `harbor.ocp1.npd.co/phoenix/`
